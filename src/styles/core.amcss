* {
  box-sizing: border-box;
}

body {
  @trait(type: sans normal, color: default);
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

button {
  background: initial;
  border: initial;
  font: inherit;
}

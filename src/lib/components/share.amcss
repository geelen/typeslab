.Share {
  @trait(layout: pt2 pb2, flex: center wrap align-center);
  > p {
    width: 100%;
    @trait(layout: pb0.5);
    > a {
      @trait(link: inline);
    }
  }
}

.ShareButton {
  @trait(layout: p0.5 m0.5, link: button);
  display: block;
  border: 1px solid;
  border-radius: 0.25rem;
}
